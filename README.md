# Weather_Eye
Weather_Eye is a real-time weather terminal using ESP8266 + DHT11. It monitors temperature, humidity, and heat index, displaying data via a responsive terminal-style web interface with live updates and a JSON API endpoint.
